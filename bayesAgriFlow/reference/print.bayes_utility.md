# Print posterior utility

Print action labels and posterior mean utilities.

## Usage

``` r
# S3 method for class 'bayes_utility'
print(x, ...)
```

## Arguments

- x:

  A `bayes_utility` object.

- ...:

  Unused additional arguments.

## Value

The input object, invisibly.

## Examples

``` r
# Example 1
if (FALSE) print(object)
# Example 2
if (FALSE) invisible(print(object))
# Example 3
if (FALSE) capture.output(print(object))
```
