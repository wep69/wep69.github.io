# Print model weights

Print the weighting method and normalized model weights.

## Usage

``` r
# S3 method for class 'bayes_model_weights'
print(x, ...)
```

## Arguments

- x:

  A `bayes_model_weights` object.

- ...:

  Unused additional arguments.

## Value

The input object, invisibly.

## Examples

``` r
# Example 1
if (FALSE) print(object)
# Example 2
if (FALSE) invisible(print(object))
# Example 3
if (FALSE) capture.output(print(object))
```
