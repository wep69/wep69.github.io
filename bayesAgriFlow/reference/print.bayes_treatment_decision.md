# Print a posterior treatment decision

Print the treatment decision and value-of-information summary.

## Usage

``` r
# S3 method for class 'bayes_treatment_decision'
print(x, ...)
```

## Arguments

- x:

  A `bayes_treatment_decision` object.

- ...:

  Unused additional arguments.

## Value

The input object, invisibly.

## Examples

``` r
# Example 1
if (FALSE) print(object)
# Example 2
if (FALSE) invisible(print(object))
# Example 3
if (FALSE) capture.output(print(object))
```
