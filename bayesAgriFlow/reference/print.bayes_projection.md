# Print projection predictive selection

Print the projection search method and stored suggested submodel size.

## Usage

``` r
# S3 method for class 'bayes_projection'
print(x, ...)
```

## Arguments

- x:

  A `bayes_projection` object.

- ...:

  Unused additional arguments.

## Value

The input object, invisibly.

## Examples

``` r
# Example 1
if (FALSE) print(object)
# Example 2
if (FALSE) invisible(print(object))
# Example 3
if (FALSE) capture.output(print(object))
```
