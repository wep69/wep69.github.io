# Print a Bayesian decision summary

Print the recommended action and ordered decision table.

## Usage

``` r
# S3 method for class 'bayes_decision'
print(x, ...)
```

## Arguments

- x:

  A `bayes_decision` object.

- ...:

  Unused additional arguments.

## Value

The input object, invisibly.

## Examples

``` r
# Example 1
if (FALSE) print(object)
# Example 2
if (FALSE) invisible(print(object))
# Example 3
if (FALSE) capture.output(print(object))
```
